<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@100;300;400;500;600;700;900&family=Montserrat:wght@100;400;500;800;900&display=swap" rel="stylesheet">
  <title>Blogisms</title>
  <link rel="stylesheet" type="text/css" href="/css/output.css">
</head>

<body class="flex flex-col justify-center content-center bg-slate-100">
  <header class="flex justify-between items-center py-5 m-0 bg-white">
    <h1 class="font-bold ml-14 text-lg">
      <a href="../../" class="">Blogisms</a>
    </h1>
    <!-- Only display the logout button if the user is logged in -->
    {{#if logged_in}}
    <nav class="">
      <button id="dashboard" class="mx-3 p-2 px-5">dashboard</button>
      <button class="mr-14 border-2 border-slate-500 rounded-lg p-2 px-5" id="logout">logout</button>
    </nav>
    {{else}}
     <nav class="">
      <a href="/login">
        <button class="mr-14 border-2 border-slate-500 rounded-lg p-2 px-5" id="login"> login</button>
      </a>
    </nav>
    {{/if}}


  </header>
  <main >
    {{{ body }}}
  </main>
  <footer>

  </footer>

  <!-- Only include the logout script if the user is logged in -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  <script src="/js/header.js"></script>
  {{/if}}

</body>

</html>
